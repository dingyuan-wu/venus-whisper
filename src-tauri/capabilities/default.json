{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: IPC commands, events, window control, opener.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
